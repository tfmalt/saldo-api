const { version } = require('../package.json');

module.exports = {
  version,
  users: {
    // Add users here
  },
  keys: [
    // add api keys here
  ],
  accounts: {
    // add accounts you want to fetch here.
  }
};
